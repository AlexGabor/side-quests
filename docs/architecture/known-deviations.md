# Known deviations

Places where the code does not follow these docs yet. **Don't copy these patterns into new code.** Remove an entry when it's fixed.

| Where                                                                                                                                                                      | Rule broken                                                                                                                                        | Target                                                                                                 |
|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------|
| [pacer/feature/home/build.gradle.kts](../../pacer/feature/home/build.gradle.kts), [pacer/feature/settings/build.gradle.kts](../../pacer/feature/settings/build.gradle.kts) | Production code depends on `test` modules (`commonMain` depends on `:lib:appstateurl:test` / `:lib:share:test` / `:pacer:core:settings:test`) so previews get fakes    | Probably need to move previews in a showcase module                                                    |
| [design/navigation](../../design/navigation)                                                                                                                               | `design/` is for Riso only                                                                                                                         | Move the whole module to `lib/navigation` (including `RisoNavigation`, so it depends on `design/riso`) |
